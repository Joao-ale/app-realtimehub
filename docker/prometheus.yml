global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'realtimehub-monitor'

scrape_configs:
  - job_name: 'realtimehub-api'
    metrics_path: '/api/actuator/prometheus'
    scrape_interval: 10s
    static_configs:
      - targets: ['api:8080']
